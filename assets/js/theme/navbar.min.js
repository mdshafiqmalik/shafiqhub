(function ($) {
  let mobToc = document.getElementById('mobToc');
  let deskToc = document.getElementById('sb');
  let shareIcons = `
    <div class="shareIcons">
     <div class="Icons">
        <a href="https://www.facebook.com/sharer.php?u=${window.location}" class="cf facebook" target="_blank" title="Share on Facebook">
          <i class="fa fa-facebook"></i>
        </a>


        <a href="https://twitter.com/intent/tweet?url=${window.location}" class="cf twitter" target="_blank" title="Share on Twitter">
          <i class="fa fa-twitter"></i>
        </a>


        <a href="https://www.linkedin.com/shareArticle?mini=true&amp;url=${window.location}" class="cf linkedin" target="_blank" title="LinkedIn">
          <i class="fa fa-linkedin"></i>
        </a>


        <a href="whatsapp://send?text=${window.location} " class="cf whatsapp" action="share/whatsapp/share" target="_blank">
          <i class="fa fa-whatsapp"></i>
        </a>
    </div>
  </div>`;
'use strict';
// -- :: Loading Page
$('html').css('overflow-y', 'visible');
// ----------------------------------
// -- :: Navbar
$(window).on('scroll', function () {
  if($(window).scrollTop() > 20 ) {
    $('.top-bar').removeClass('d-lg-block');
    $('.sp').css('top','.5rem');
    $('nav').addClass('scrolled');
  }else {
    $('nav').removeClass('scrolled');
    $('.top-bar').addClass('d-lg-block');
    $('.sp').css('top','auto');
  }
});
// Posts
  window.addEventListener("resize", responsiveToc);
  function responsiveToc(){
      if($(document).width() < 991) {
          mobToc.innerHTML = shareIcons + breadCrumb;
          deskToc.innerHTML = "";
      }
      else{
        mobToc.innerHTML = "";
        deskToc.innerHTML = shareIcons + breadCrumb;
      }
  }


$(window).on('scroll', function (){
  if($(window).scrollTop() > 430 ) {
      $(".contents").addClass("abs");
      $('.contents').css('padding-bottom','0.3rem');
      $("#mobToc label:first-child").css("display", "none");
  }else {
      $(".contents").removeClass("abs");
      $('.contents').css('padding-bottom','0.6rem');
      $("#mobToc label:first-child").css("display", "inline");
  }
});
$('nav button').on('click', function() {
  $($(this).data('target')).fadeToggle();
});


document.addEventListener("DOMContentLoaded",function(){
    responsiveToc();

  if ($(document).width() < 974) {
    // Add slideDown animation to Bootstrap dropdown when expanding.
    $('.dropdown').on('show.bs.dropdown', function() {
      $(this).find('.dropdown-menu').first().stop(true, true).slideDown();
    });

    // Add slideUp animation to Bootstrap dropdown when collapsing.
    $('.dropdown').on('hide.bs.dropdown', function() {
      $(this).find('.dropdown-menu').first().stop(true, true).slideUp();
    });
  } else {
    return false;
  }
});
// Widnow Location jQuery
}(jQuery));
